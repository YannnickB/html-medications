<head>

	<!-- Required meta tags -->
	<meta charset="">
	<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

	<!-- Bootstrap CSS -->
	<link rel="stylesheet" href="<{$config.rootUrl}>/node_modules/bootstrap/dist/css/bootstrap.min.css">


	<!-- Font Awesome -->
	<link rel="stylesheet" href="<{$config.rootUrl}>/node_modules/@fortawesome/fontawesome-free/css/all.min.css"/>

	<!-- Toastr -->
	<link href="<{$config.rootUrl}>/node_modules/toastr/build/toastr.min.css" rel="stylesheet"/>
	
	
	
	<!-- Page title -->
	<title>PHP - PDO Structure<{*if $cmsPage.title}><{$cmsPage.title}><{else}><{$locales.app_name}><{/if*}></title>
	
	<!-- Page icon SVG-->
	<{*if $config.website.tabIconSvgUrl}><link rel="icon" type="image/svg" href="<{$config.resourcesUrl}>/<{$config.website.tabIconSvgUrl}>" /><{/if*}>
	
</head>
